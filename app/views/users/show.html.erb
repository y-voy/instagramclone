<h1><%= @user.name %>のページ</h1>
<p>メールアドレス: <%= @user.email %></p>
<p>画像: <%= image_tag(@user.image.url) if @user.image && @user.image.url %></p>

<%= link_to "編集する", edit_user_path(@user.id) %>
<%= link_to '投稿一覧へ', pictures_path, class: "btn btn-primary"%>
